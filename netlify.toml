# Netlify Configuration File
# Configures serverless functions and routes /api/* to /.netlify/functions/*

[build]
  publish = "."

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
